/// <reference types="vite/client" />
// Needed for the `?raw` import in features/backtest/interactiveHtml.ts (inlines the
// lightweight-charts standalone bundle into the self-contained HTML export).
