# คู่มือดูแลระบบ SN·Desk (Structured Note Summary)

> **สำหรับใคร:** ทีมเซลล์ / IC ที่ใช้งานระบบนี้ต่อ หลังผู้พัฒนา (เด็กฝึกงานสาย IT) จบการฝึกงาน
> **ไม่ต้องเขียนโค้ดเป็น** — ทำตามคู่มือนี้ได้เลย ส่วนที่ต้องแก้โค้ดให้ Claude Code ทำ (ดู §5)
> **เอกสารพี่น้อง:** `README.md` (ภาพรวม) · `SETUP.md` (ติดตั้งเครื่องใหม่) · `HANDOVER.md` (รายละเอียดเชิงเทคนิคทั้งหมด)
>
> อัปเดตล่าสุด: 4 สิงหาคม 2569

---

## 1. ระบบนี้คืออะไร และมีข้อจำกัดอะไรที่ต้องรู้ก่อน

**หน้าที่ระบบ:** รับ Term Sheet (ไฟล์ PDF/รูป หรือข้อความที่ก๊อปจากไลน์/อีเมล) → AI สกัดเงื่อนไข → แบ็คเทสต์กับราคาปิดจริงย้อนหลัง → จัดอันดับ → ต่อยอดเป็น Script ขาย / Factsheet / กราฟ / ZIP ส่งลูกค้า

**ข้อจำกัดที่ต้องเข้าใจตั้งแต่ต้น (สำคัญมาก):**

| เรื่อง | ความจริง |
|--------|----------|
| ระบบรันที่ไหน | **บนเครื่องคอมพิวเตอร์เครื่องนี้เท่านั้น** ไม่ได้อยู่บนเซิร์ฟเวอร์/คลาวด์ |
| ปิดเครื่อง / เครื่องหลับ | ระบบดับ คนอื่นเปิดเว็บไม่ได้ ต้องเปิดสคริปต์ใหม่ (§2) |
| คนอื่นเข้าใช้จากเครื่องตัวเองได้ไหม | **ไม่ได้** — ล็อกไว้ที่ `localhost` ตัวเครื่องนี้ |
| AI ใช้เงินจากไหน | **subscription Claude Code ที่ล็อกอินอยู่บนเครื่องนี้** ไม่มี API key ถ้าล็อกอินหลุด AI จะไม่ทำงานทั้งระบบ |
| ราคาหุ้นมาจากไหน | Yahoo Finance (ฟรี ไม่ต้องมี key) — ถ้า Yahoo ล่มหรือจำกัดการเรียก กราฟจะโหลดไม่ขึ้นชั่วคราว |
| ข้อมูลลูกค้า | **ห้ามใส่ชื่อ/เลขบัญชี/ข้อมูลส่วนตัวลูกค้าลงในช่องข้อความ** ระบบส่งข้อความไปให้ AI ประมวลผล ให้ใส่แค่เงื่อนไขผลิตภัณฑ์ |

---

## 2. ใช้งานประจำวัน — เปิด / ปิด

### เปิดระบบ
1. ไปที่โฟลเดอร์ `C:\Intern Project\KIKO-graph`
2. คลิกขวาไฟล์ **`start-sndesk.ps1`** → **Run with PowerShell**
3. รอ 10–30 วินาที เบราว์เซอร์จะเปิด `http://localhost:5173` ให้เอง

หน้าต่างดำ (PowerShell) 2 อันจะย่อไว้ที่ taskbar — **ห้ามปิด** ปิดแล้วระบบดับ

ถ้าคลิกขวาไม่มีเมนู Run with PowerShell: เปิด PowerShell แล้วพิมพ์
```powershell
cd "C:\Intern Project\KIKO-graph"
.\start-sndesk.ps1
```

### ปิดระบบ
คลิกขวา **`stop-sndesk.ps1`** → Run with PowerShell (ปิดทั้ง backend และ frontend ให้เรียบร้อย)

### เช็คว่าระบบปกติ (30 วินาที)
| เช็ค | ผลที่ควรได้ | ถ้าไม่ได้ |
|------|-------------|-----------|
| เปิด `http://localhost:8000/api/generate/health` | `{"available": true, ...}` | ดู §4 อาการ B |
| หน้าเว็บ `http://localhost:5173` ขึ้นปกติ | เห็นหน้า Structured Note Summary | ดู §4 อาการ A |
| ไม่มีแบนเนอร์แดง "เชื่อมต่อ backend ไม่ได้" | ไม่มีแถบแดง | ดู §4 อาการ A |

---

## 3. ไฟล์ที่ระบบสร้างให้ เก็บไว้ที่ไหน

| ปุ่ม | ได้อะไร | ไปโผล่ที่ |
|------|---------|-----------|
| ดาวน์โหลด ZIP ทั้งชุด | `summary.csv` + โฟลเดอร์รายผลิตภัณฑ์ (PDF + JPG + factsheet) + โฟลเดอร์ **`all-images-png`** รวมรูปทุกผลิตภัณฑ์ไว้ที่เดียว (ลากส่งไลน์ได้เลย) | `Downloads\SN-Desk-batch-<วันที่>\` |
| ส่งอีเมลอัตโนมัติ | ส่งจริงผ่าน Outlook พร้อมรูป ranking + ZIP | Sent Items ของ Outlook |
| สร้าง Factsheet | หน้า A4 ไทย/อังกฤษ พิมพ์หรือบันทึก PDF ได้ | ตามที่เลือกตอนบันทึก |

**ต้องมี Classic Outlook เปิดและล็อกอินอยู่** ปุ่มส่งอีเมลจึงทำงาน (ฟีเจอร์นี้ใช้ได้เฉพาะ Windows)

---

## 4. อาการเสีย → สาเหตุ → วิธีแก้ด้วยตัวเอง

ทำตามลำดับ ถ้าถึงข้อสุดท้ายแล้วยังไม่หาย ให้ไปที่ §5 (เรียก Claude Code)

### A. หน้าเว็บขึ้นแถบแดง "เชื่อมต่อ backend ไม่ได้ (localhost:8000)"
1. รัน `stop-sndesk.ps1` แล้วรัน `start-sndesk.ps1` ใหม่ (แก้ได้ 90% ของเคส)
2. ถ้าสคริปต์ขึ้น `[X] venv missing` → environment ของ backend หาย ต้องติดตั้งใหม่ตาม `SETUP.md` ข้อ 3.2 (งานนี้ให้ Claude Code ทำ)

### B. `health` ขึ้น `{"available": false}` หรือ AI ไม่ตอบทุกปุ่ม
สาเหตุ = ระบบเรียก Claude Code ไม่ได้
1. เปิด PowerShell พิมพ์ `claude --version` → **ต้องได้บรรทัดที่ลงท้ายด้วย `(Claude Code)`**
2. ถ้าได้เลข version เปล่าๆ / error `output is not defined` → ตัวโปรแกรม `claude.exe` เสีย (เคยเกิด 2 ครั้ง หลัง npm อัปเดตอัตโนมัติ) → มีวิธีกู้ใน `HANDOVER.md` §7 มี backup อยู่ที่ `bin/claude.exe.old.<เวลา>` — **ให้ Claude Code ทำ** (§5)
3. ถ้า version ปกติแต่ยัง false → พิมพ์ `claude login` แล้วล็อกอินใหม่ (subscription หมดอายุ/ถูกเด้งออกก็เข้าเคสนี้)

### C. สกัดข้อมูลไม่ขึ้นเลย / ได้ 0 ผลิตภัณฑ์
1. ดูแถบเหลือง "พบปัญหาในการสกัดข้อมูล ..." บนแดชบอร์ด — ข้อความในนั้นบอกสาเหตุจริง (เดิมระบบเงียบ ตอนนี้บอกแล้ว)
2. ถ้าเป็น timeout: เอกสาร/ข้อความยาวเกินไป → แบ่งวางเป็น 2–3 รอบ (AI มีเพดาน 180 วินาทีต่อครั้ง)
3. ลองกดวิเคราะห์ซ้ำ 1 ครั้ง — โมเดลตอบผิดรูปเป็นครั้งคราวได้

### D. กราฟราคาไม่ขึ้น / ขึ้นว่า "ไม่มีข้อมูลราคา XXX"
1. **ชื่อหุ้นผิด** — เช็คว่าตัวย่อในเอกสารตรงกับที่ Yahoo ใช้
2. **เป็นดัชนี** (SPX, NDX, SET ฯลฯ) — ระบบแปลงชื่อให้แล้ว แต่ถ้าเจอดัชนีตัวใหม่ที่ยังไม่มีในตาราง ต้องเพิ่ม (งานของ Claude Code — ตารางอยู่ที่ `backend/main.py` ชื่อ `INDEX_SYMBOL_MAP`)
3. **ขึ้น 429 / rate limit** — Yahoo กันเรียกถี่ รอ 2–3 นาทีแล้วลองใหม่ ไม่ใช่ระบบเสีย

### E. ปุ่มส่งอีเมลไม่ทำงาน
1. เปิด Classic Outlook ให้ล็อกอินเสร็จก่อน แล้วกดส่งอีก (ระบบลองซ้ำเองอยู่แล้ว 3 ครั้ง ห่างกัน 5 วินาที)
2. ใช้ New Outlook อยู่ → ไม่รองรับ ต้องใช้ Classic Outlook

### F. ตัวเลขบนกราฟ/Factsheet ดูไม่ตรงกับ Term Sheet
เช็คตรงนี้ก่อนโทษระบบ:
- **เอกสารไม่ระบุวันทำสัญญา (trade/fixing date)** → ระบบใช้ **ราคาปิดล่าสุด** เป็นราคาอ้างอิง Strike/KI/KO จึงคิดจากราคาวันนี้ และหมุดวันสังเกตการณ์จะเขียน `obs1*` (มีดอกจัน = วันที่สมมติจากความถี่ในเอกสาร) — รูปที่ export จะมีกล่องเตือนสีเหลืองบอกไว้แล้ว **ถ้าจะส่งลูกค้าจริงให้ใส่วันทำสัญญาในข้อความต้นทางก่อน**
- **ผลิตภัณฑ์ที่ไม่ใช่ KIKO** (Sharkfin / Twin Win / BEN / Booster) — ผล "Pass / Knocked" **ยังเชื่อไม่ได้** ระบบยังคิดด้วยสูตร KIKO worst-of สูตรเฉพาะโครงสร้างยังไม่ได้ทำ (ดู §7) ตารางจัดอันดับจึงตัดพวกนี้ออกไปไว้ตาราง "ผลิตภัณฑ์อื่น" โดยตั้งใจ
- **💎 INVX Pick** ขึ้นเฉพาะเมื่อเอกสารต้นทางเขียนกำกับมา ระบบไม่ตัดสินเอง

---

## 5. แก้เองไม่ได้ → เรียก Claude Code (ขั้นตอนหลัก)

1. เปิดโปรแกรม **Claude Code**
2. เลือกโปรเจกต์ **`Structure Note Summary Website (N'Nina)`**
3. ก๊อปข้อความข้างล่างไปวาง แล้วเติมอาการจริงลงไป

```
ระบบ SN·Desk ใช้ไม่ได้ ช่วยหาสาเหตุและแก้ให้ด้วย

อาการ: (เช่น กดวิเคราะห์แล้วขึ้นแถบแดง เชื่อมต่อ backend ไม่ได้)
ทำอะไรอยู่ตอนเจอ: (เช่น วางข้อความ 5 ผลิตภัณฑ์แล้วกดเริ่มวิเคราะห์)
ข้อความ error ที่เห็น: (ก๊อปมาทั้งบรรทัด หรือแนบรูป)
ลองแก้อะไรไปแล้ว: (เช่น รัน stop แล้ว start ใหม่)

อ่าน MAINTENANCE.md, SETUP.md, HANDOVER.md ในโปรเจกต์นี้ก่อน
แก้เสร็จบอกด้วยว่าแก้ไฟล์ไหน และครั้งหน้าถ้าเจออีกต้องทำอะไร
```

**เคล็ดลับการสั่งงาน:**
- แนบรูปหน้าจอได้เลย (ลากไฟล์รูปใส่ช่องแชท) — เร็วกว่าอธิบายด้วยคำ
- อยากได้ฟีเจอร์ใหม่ ก็บอกเป็นภาษาคนได้ เช่น "อยากให้ ZIP มีโฟลเดอร์รวมรูป PNG ทุกผลิตภัณฑ์"
- ถ้ามันแก้แล้วยังไม่ถูก บอกว่า "ยังไม่ถูก ผลที่เห็นคือ ... ที่ต้องการคือ ..." อย่าเริ่มใหม่จากศูนย์
- **ก่อนปิดงานทุกครั้ง สั่งว่า "commit งานที่แก้ด้วย"** เพื่อให้มีประวัติย้อนกลับได้
- อย่าให้ Claude Code แก้อะไรตอนเซลล์กำลังใช้งานจริง — ให้รัน `stop-sndesk.ps1` ก่อน

---

## 6. งานดูแลตามรอบ (ทำแล้วระบบไม่ตายกลางทาง)

### ทุกสัปดาห์ (2 นาที)
- [ ] เปิด `http://localhost:8000/api/generate/health` → ต้องเป็น `true`
- [ ] ทดลองวิเคราะห์ 1 ผลิตภัณฑ์ให้ครบจบถึงกราฟ

### ทุกเดือน (10 นาที — ให้ Claude Code ช่วย)
- [ ] `claude --version` ยังลงท้าย `(Claude Code)` (จุดพังบ่อยที่สุดของระบบนี้)
- [ ] สั่ง Claude Code: "commit งานที่ค้างใน working tree ให้เรียบร้อย"
- [ ] เช็คพื้นที่ดิสก์ ลบโฟลเดอร์ `SN-Desk-batch-*` เก่าใน Downloads
- [ ] ถ้าระบบเริ่มอืดผิดปกติ: ล้าง cache ในเบราว์เซอร์ (ระบบเก็บผลสกัดไว้ใน localStorage เพื่อไม่ต้องจ่ายโทเคนซ้ำ)

### ทุกไตรมาส / เมื่อเครื่องถูกเปลี่ยน
- [ ] มีคนเดียวที่รู้เรื่องระบบนี้ = ความเสี่ยง ให้เพิ่มคนที่รู้วิธี §2 และ §5 อย่างน้อย 2 คน
- [ ] ถ้าต้องย้ายเครื่องใหม่ ใช้ `SETUP.md` ทั้งไฟล์ (สั่ง Claude Code ให้ทำตามไฟล์นั้นทีละขั้น)

---

## 7. ห้ามทำ (เคยพังมาแล้ว หรือพังแน่)

| ห้าม | เพราะ |
|------|-------|
| ปิดหน้าต่าง PowerShell ที่ย่อไว้ระหว่างใช้งาน | ระบบดับทันที |
| อัปเดต Node/npm/Claude Code เองตอนไม่มีคนดูแล | `claude.exe` เคยเสียจาก npm auto-update 2 ครั้ง ทำ AI ล่มทั้งระบบ |
| แก้เลขพอร์ต 8000 / 5173 | ต้องแก้พร้อมกันหลายไฟล์ ไม่ครบ = ระบบไม่คุยกัน (รายละเอียด `SETUP.md` §5) |
| ลบโฟลเดอร์ `node_modules`, `backend/.venv`, `.git` | ต้องติดตั้งใหม่ทั้งชุด / ประวัติโค้ดหาย |
| ส่ง Factsheet หรือรูปที่มีกล่องเตือนสีเหลืองให้ลูกค้าโดยไม่อ่าน | กล่องนั้นบอกว่าเลขบางตัวเป็นค่าสมมติ (ไม่มีวันทำสัญญาในเอกสาร) |
| ใช้ผล Pass/Knocked ของสินค้าที่ไม่ใช่ KIKO ไปคุยลูกค้า | สูตรแบ็คเทสต์เฉพาะโครงสร้างยังไม่ได้ทำ (§8) |

---

## 8. งานที่ยังไม่เสร็จ (ฝากคนถัดไป)

เรียงตามความสำคัญต่อการใช้งานจริง:

1. **สูตรแบ็คเทสต์ตามโครงสร้าง** — Bearish Sharkfin ต้องเช็คทะลุ**ลง** (โค้ดปัจจุบันเช็คขึ้น), Twin Win ต้องรองรับ Upper/Lower KO คู่, สินค้าไม่มี KI ตอนนี้ขึ้น "Pass" ตลอด → ทำให้ตาราง "ผลิตภัณฑ์อื่น" เชื่อถือได้
2. **Factsheet ตระกูล BEN โชว์ Bonus Coupon เป็น "—"** — `adapter.ts` ไม่ได้ map `bonus` / `couponBarrier` ลง `deal.levels` (มี diagnosis พร้อมใน `SETUP.md` §7)
3. **Factsheet KIKO อธิบายการสังเกต KI/KO ผิด** เมื่อ note เป็นแบบ final-valuation — `backfillFromProduct` ไม่ส่ง `kiType`/`koType` ต่อ
4. **คะแนน Buffer ÷ Volatility** — ตอนนี้อันดับของแต่ละโปรไฟล์ออกมาใกล้กันเกินไป (มี diagnosis แล้ว pause ไว้)
5. **ย้ายขึ้นโฮสต์จริง** — ต้องหาทางแทน `claude -p` ที่วิ่งบนเครื่อง local ก่อน ถึงจะให้หลายคนใช้พร้อมกันได้

ทั้งหมดนี้สั่ง Claude Code ทำได้ โดยอ้างหัวข้อนี้ตรงๆ เช่น "ทำข้อ 2 ใน MAINTENANCE.md §8 ให้"

---

## 9. ติดต่อ / ผู้รับผิดชอบ

| บทบาท | ชื่อ | ช่องทาง |
|-------|------|---------|
| ผู้พัฒนาเดิม (intern) | (เติมชื่อ) | (เติมช่องทาง — ระบุด้วยว่าติดต่อได้ถึงเมื่อไหร่) |
| เจ้าของระบบฝั่งธุรกิจ | (เติมชื่อทีมเซลล์ที่ดูแล) | — |
| IT ภายใน (เครื่อง/Outlook/สิทธิ์) | (เติม) | — |

> ผู้พัฒนาเดิมจบฝึกงานแล้ว **ไม่ควรพึ่งคนเดียว** — ทางแก้หลักคือ Claude Code (§5) เอกสาร 4 ไฟล์ในโปรเจกต์นี้เขียนไว้ให้ AI อ่านเองได้
